services:
  api:
    build: .
    env_file:
      - path: .env
        required: false
    environment:
      ENVIRONMENT: production
    ports:
      - "8000:8000"
    volumes:
      - rag_data:/app/data
    restart: unless-stopped

volumes:
  rag_data:
