.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
data/uploads/
data/registry.json
qdrant_storage/
.wgm
.tokensave
.codex
docs/research
.pytest_tmp/
.codebase-memory
specs/
reports/rag_evaluation/

# Dev/eval scratch collections and their registries + upload copies.
# The prod paths above only cover the exact production names, so parallel
# Blue-Green builds would otherwise be swept in by `git add -A`.
data/registry_*.json
data/uploads_*/
reports/
